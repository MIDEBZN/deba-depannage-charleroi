---
---
<section class="trust-bar bg-light">
  <div class="container">
    <div class="trust-grid">
      
      <div class="trust-item">
        <div class="trust-icon">⏱️</div>
        <div class="trust-content">
          <h4>Intervention Rapide</h4>
          <p>Service de dépannage réactif sur tout le Grand Charleroi.</p>
        </div>
      </div>

      <div class="trust-item">
        <div class="trust-icon">🏆</div>
        <div class="trust-content">
          <h4>Artisan Local</h4>
          <p>Savoir-faire reconnu et expertise de terrain à Charleroi.</p>
        </div>
      </div>

      <div class="trust-item">
        <div class="trust-icon">📜</div>
        <div class="trust-content">
          <h4>Devis Transparent</h4>
          <p>Tarifs annoncés à l'avance, sans mauvaises surprises.</p>
        </div>
      </div>

      <div class="trust-item">
        <div class="trust-icon">🛡️</div>
        <div class="trust-content">
          <h4>Travail Soigné</h4>
          <p>Des installations durables et un respect strict des normes en vigueur.</p>
        </div>
      </div>

    </div>
  </div>
</section>

<style>
  .trust-bar {
    background-color: var(--bg-light, #f8f9fa);
    padding: 40px 0;
    border-top: 1px solid rgba(0,0,0,0.05);
    border-bottom: 1px solid rgba(0,0,0,0.05);
  }
  
  .trust-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 30px;
  }
  
  .trust-item {
    display: flex;
    align-items: flex-start;
    gap: 15px;
  }
  
  .trust-icon {
    font-size: 32px;
    line-height: 1;
    color: var(--primary);
  }
  
  .trust-content h4 {
    font-size: 1rem;
    margin: 0 0 5px 0;
    color: var(--secondary);
  }
  
  .trust-content p {
    font-size: 0.85rem;
    margin: 0;
    color: var(--text-muted);
    line-height: 1.5;
  }
  
  @media (max-width: 768px) {
    .trust-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
  
  @media (max-width: 480px) {
    .trust-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
